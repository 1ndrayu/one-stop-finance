# PostgreSQL database connection
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/asset_token_engine

# App settings
APP_NAME=OneStopFinance
APP_ENV=development
DEBUG=True

# Token settings (for asset/token UID generation or authentication)
SECRET_KEY=your_super_secret_key

# CORS settings (for frontend integration)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
